Перем юТест;

Функция ПолучитьСписокТестов(ЮнитТестирование) Экспорт
	
	юТест = ЮнитТестирование;
	
	ВсеТесты = Новый Массив;

	ВсеТесты.Добавить("Тест_ФормаПоУмолчаниюДолжнаБытьОпределена");

	Возврат ВсеТесты;
	
КонецФункции

Процедура ПередЗапускомТеста() Экспорт
	
	//юТест.ВывестиСообщение("Перед запуском теста");
	
КонецПроцедуры

Процедура ПослеЗапускаТеста() Экспорт
	
	//юТест.ВывестиСообщение("После запуска теста");
	
КонецПроцедуры

Процедура Тест_ФормаПоУмолчаниюДолжнаБытьОпределена() Экспорт
	
	файл = Новый Файл(ЭтотОбъект.ИспользуемоеИмяФайла);
	
	Обработка = юТест.СоздатьОбъектВнешнейОбработкиОтключаяБезопасныйРежим(файл.Путь+ "..\xddGuidShow.epf");
	НоваяФорма = Обработка.ПолучитьФорму("Форма");
	Если НоваяФорма = Неопределено Тогда
		ВызватьИсключение("Инструмент xddGuidShow.epf : не удалось получить основную форму!");
	КонецЕсли;
	
КонецПроцедуры
