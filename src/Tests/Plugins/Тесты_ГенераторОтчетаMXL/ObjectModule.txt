Перем КонтекстЯдра;
Перем Ожидаем;

Процедура Инициализация(КонтекстЯдраПараметр) Экспорт
	КонтекстЯдра = КонтекстЯдраПараметр;
	Ожидаем = КонтекстЯдра.Плагин("УтвержденияBDD");
КонецПроцедуры

Функция ПолучитьСписокТестов() Экспорт
	ВсеТесты = Новый Массив;
	ВсеТесты.Добавить("ТестДолжен_ПолучитьОтчетОТестировании");
	
	Возврат ВсеТесты;
КонецФункции

Процедура ПередЗапускомТеста() Экспорт
	
КонецПроцедуры

Процедура ПослеЗапускаТеста() Экспорт
	
КонецПроцедуры

//Процедура ТестДолжен_ПолучитьОтчетОТестировании() Экспорт
//	СостоянияТестов = КонтекстЯдра.СостоянияТестов();
//	
//	РезультатМетод1 = КонтекстЯдра.НовыйРезультатТестирования("Путь1", "Метод1");
//	РезультатМетод1.Состояние = СостоянияТестов.Пройден;
//	РезультатМетод2 = КонтекстЯдра.НовыйРезультатТестирования("Путь1", "Метод2");
//	РезультатМетод2.Состояние = СостоянияТестов.НеРеализован;
//	РезультатМетод2.Сообщение = "Не реализован";
//	РезультатМетод3 = КонтекстЯдра.НовыйРезультатТестирования("Путь1", "Метод3");
//	РезультатМетод3.Состояние = СостоянияТестов.Сломан;
//	РезультатМетод3.Сообщение = "БАБАХ";
//	РезультатМетод4 = КонтекстЯдра.НовыйРезультатТестирования("Путь1", "Метод4");
//	
//	РезультатТестирования = КонтекстЯдра.НоваяГруппаРезультатовТестирования("Корень");
//	РезультатТестирования.Состояние = СостоянияТестов.Сломан;
//	РезультатТестирования.Строки.Добавить(РезультатМетод1);
//	РезультатТестирования.Строки.Добавить(РезультатМетод2);
//	РезультатТестирования.Строки.Добавить(РезультатМетод3);
//	РезультатТестирования.Строки.Добавить(РезультатМетод4);
//	
//	ГенераторОтчетаMXL = КонтекстЯдра.Плагин("ГенераторОтчетаMXL");
//	Отчет = ГенераторОтчетаMXL.СоздатьОтчет(РезультатТестирования);
//	
//	ВызватьИсключение "Тест не реализован";
//КонецПроцедуры
